{
  "name": "@his-ignite-ui/global",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo run build --filter=@his-ignite-ui/docs && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.8",
    "turbo": "^2.0.10"
  },
  "packageManager": "npm@10.7.0",
  "dependencies": {
    "@stitches/react": "^1.2.8"
  }
}
